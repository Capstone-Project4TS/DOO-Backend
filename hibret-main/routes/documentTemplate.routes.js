// routes/documentTemplates.js

import { Router } from 'express';
const router = Router();
//import { createDocumentTemplate, getAllDocumentTemplates, getDocumentTemplateById, updateDocumentTemplateById, deleteDocumentTemplateById, getDocumentsByTemplateId } from '../controllers/documentTemplateController';

// // Route for creating a new document template
// router.post('/', createDocumentTemplate);

// // Route for retrieving all document templates
// router.get('/', getAllDocumentTemplates);

// // Route for retrieving a specific document template by ID
// router.get('/:id', getDocumentTemplateById);

// // Route for updating a document template by ID
// router.put('/:id', updateDocumentTemplateById);

// // Route for deleting a document template by ID
// router.delete('/:id', deleteDocumentTemplateById);

// // Route for retrieving documents based on a document template
// router.get('/:templateId/documents', getDocumentsByTemplateId);

export default router;
